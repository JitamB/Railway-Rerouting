# Redis config for the CascadeGuard store-and-forward buffer / event bus (skeleton).
# Redis Streams is the deliberate choice over Kafka for a single-zone build (audit-02 §3.6).
appendonly yes
appendfsync everysec
# Streams are created at runtime under key CG_STREAM_KEY (see .env.example).
