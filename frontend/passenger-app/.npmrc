# React 19 / Expo SDK 54: the RN ecosystem still has peer-dependency lag, so npm's strict
# peer resolution (ERESOLVE) fails. Expo ships this setting in SDK 53+ projects by default.
legacy-peer-deps=true
